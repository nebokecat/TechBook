    <% if current_user.following?(@user) %>
      <%= link_to "フォロー外す", relationships_path(@user.name), method: :delete,remote: true, class:"btn btn-default"%>
    <% else %>
      <%= link_to "フォローする", relationships_path(@user.name), method: :post, remote: true,class:"btn btn-primary"%>
    <% end %>